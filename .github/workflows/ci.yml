name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Permissions needed across all jobs.
# Kept broad at the top level so the GITHUB_TOKEN passed to the reusable
# workflow has sufficient rights. Jobs that don't need write access are not
# further restricted — this is a single-maintainer project.
permissions:
  contents: write        # semantic-release: create tags + GitHub releases
  packages: write        # ghcr.io push
  issues: write          # semantic-release: close referenced issues
  pull-requests: write   # semantic-release: comment on PRs

jobs:
  # ── 1. Compute image tags based on event type ─────────────────────────────
  prepare:
    runs-on: ubuntu-24.04
    outputs:
      tags:     ${{ steps.compute.outputs.tags }}
      scan-ref: ${{ steps.compute.outputs.scan-ref }}
    steps:
      - name: Compute image tags
        id: compute
        run: |
          SHA="${GITHUB_SHA::8}"
          IMAGE="ghcr.io/${{ github.repository }}"
          IMAGE="${IMAGE,,}"   # OCI requires lowercase

          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            SCAN_REF="${IMAGE}:pr-${{ github.event.number }}-${SHA}"
            TAGS="${SCAN_REF}"
          else
            SCAN_REF="${IMAGE}:main-${SHA}"
            TAGS="${SCAN_REF}"$'\n'"${IMAGE}:main"
          fi

          echo "scan-ref=${SCAN_REF}" >> "$GITHUB_OUTPUT"
          {
            echo "tags<<EOF"
            echo "${TAGS}"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

  # ── 2. Build, push, scan (reusable) ─────────────────────────────────────
  build-push-scan:
    needs: prepare
    uses: ./.github/workflows/_build-scan.yml
    with:
      tags:     ${{ needs.prepare.outputs.tags }}
      scan-ref: ${{ needs.prepare.outputs.scan-ref }}
    secrets: inherit

  # ── 3. Semantic release + image tagging ─────────────────────────────────
  release:
    needs: [prepare, build-push-scan]
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout (full history for semantic-release)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Semantic Release
        id: semantic
        uses: cycjimmy/semantic-release-action@v4
        with:
          # On PR: dry-run previews the next version without publishing.
          # On main: creates the GitHub release and git tag.
          dry_run: ${{ github.event_name == 'pull_request' }}
          extra_plugins: |
            @semantic-release/commit-analyzer
            @semantic-release/release-notes-generator
            @semantic-release/github
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Tag release image (vX.X.X + latest)
        if: steps.semantic.outputs.new_release_published == 'true'
        env:
          SRC:     ${{ needs.prepare.outputs.scan-ref }}
          VERSION: ${{ steps.semantic.outputs.new_release_version }}
          IMAGE:   ghcr.io/${{ github.repository }}
        run: |
          IMAGE="${IMAGE,,}"
          echo "${{ secrets.GITHUB_TOKEN }}" | skopeo login ghcr.io -u "${{ github.actor }}" --password-stdin
          skopeo copy "docker://${SRC}" "docker://${IMAGE}:v${VERSION}"
          skopeo copy "docker://${SRC}" "docker://${IMAGE}:latest"
